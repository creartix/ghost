{{!< default}}

{{#tag}}
    <div class="h-[30vh] lg:h-[50vh] opacity-30 absolute top-0 left-0 right-0 -z-1 mask-cover mask-no-repeat [mask-image:linear-gradient(to_top,transparent_0%,rgba(0,0,0,0.25)_40%,rgba(0,0,0,0.75)_70%,black_100%)]">
        <img
            class="w-full h-full object-cover"
            srcset="{{img_url feature_image size="s"}} 300w,
            {{img_url feature_image size="m" }} 600w,
            {{img_url feature_image size="l" }} 1200w,
            {{img_url feature_image size="xl" }} 2000w"
            sizes="100vw"
            src="{{img_url feature_image size="l"}}"
            alt="{{primary_tag.name}}"
            loading="eager"
            fetchpriority="high"
        />
    </div>

    <div class="max-w-5xl mx-auto px-8 py-12 text-center">
        <h1 class="font-headline font-light text-3xl text-black leading-tight lg:text-5xl dark:text-white">
            <span class="font-semibold">{{name}}</span>
        </h1>
        <p class="mt-4 font-headline font-light text-xl text-neutral-800 lg:text-2xl dark:text-neutral-100">
            {{#if description}}
                {{description}}
            {{else}}
                Eine Sammlung von {{plural ../pagination.total empty='Beiträgen' singular='% Beitrag' plural='% Beiträgen'}}
            {{/if}}
        </p>
    </div>
{{/tag}}

<section class="py-16 px-6">
    <div class="max-w-8xl mx-auto">
        <section>
            <h2 class="mb-4 font-medium uppercase text-xs dark:text-neutral-400">
                {{#tag}}
                {{plural ../pagination.total empty='Beiträgen' singular='% Beitrag' plural='% Beiträgen'}} über {{name}}
                {{/tag}}
            </h2>
            <div class="@container grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-10">
                {{#foreach posts}}
                    {{> "post-card"}}
                {{/foreach}}
            </div>
        </section>
    </div>
</section>

